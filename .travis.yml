language: ruby

rvm:
  - 1.9.3
  - 2.0.0
  - 2.1

env:
  -
  - RACK_VERSION=1.1.0
  - RACK_VERSION=1.2.0
  - RACK_VERSION=1.3.0
  - RACK_VERSION=1.4.0
  - RAILS_VERSION=3.2.0
  - RAILS_VERSION=4.0.0
  - RAILS_VERSION=4.1.0

script: "bundle exec rake spec"

branches:
  only:
    - master
